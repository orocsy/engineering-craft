# Claude Code Instructions for {{PROJECT_NAME}}

> **Template generated by `engineering-craft/bootstrap/templates/project-CLAUDE.md`.**
> Replace `{{PLACEHOLDERS}}` with project-specific values, delete sections that don't apply,
> and check this file into the project repo at `.claude/CLAUDE.md` (Claude Code reads it
> automatically on every session start).

**Project**: {{PROJECT_NAME}} — {{ONE_LINE_DESCRIPTION}}
**Stack**: {{TECH_STACK}}
**Workflow**: dev-pipeline plugin (auto-loaded) — see plugin's own `CLAUDE.md` and `commands/` for routing tables, gate enforcement, MIU decomposition, validation, deliver flow.

This file is intentionally **thin**. It contains only:
- Project-specific safety rules (non-negotiable invariants)
- Project structural facts (folder layout, monorepo commands, env-var pattern)
- Pointers to deeper docs (Layer 2 business knowledge, design docs)

It does NOT duplicate engineering-craft skill content (auto-loaded based on diff keywords).
It does NOT duplicate dev-pipeline plugin content (workflow flows live in the plugin).

User-level Agent Directives at `~/.claude/CLAUDE.md` apply to every action; they are non-overridable.

---

## CRITICAL: {{INVARIANT_1_NAME}} (project-specific, non-negotiable)

**{{ONE_SENTENCE_RULE}}**

```{{LANG}}
// ❌ Counter-example
{{COUNTER_EXAMPLE_CODE}}

// ✅ Correct
{{CORRECT_EXAMPLE_CODE}}
```

Per-feature checklist:
- {{CHECKLIST_ITEM_1}}
- {{CHECKLIST_ITEM_2}}
- {{CHECKLIST_ITEM_3}}

**Detailed Layer 2 reference**: [`docs/business-knowledge/{{INVARIANT_1_SLUG}}.md`](../docs/business-knowledge/{{INVARIANT_1_SLUG}}.md)
**Code reference**: `{{CODE_REFERENCE_PATH}}`

---

## CRITICAL: {{INVARIANT_2_NAME}} (project-specific, non-negotiable)

**{{ONE_SENTENCE_RULE}}**

```{{LANG}}
{{EXAMPLE_PATTERN}}
```

**Detailed Layer 2 reference**: [`docs/business-knowledge/{{INVARIANT_2_SLUG}}.md`](../docs/business-knowledge/{{INVARIANT_2_SLUG}}.md)

---

## Project structure

```
{{PROJECT_NAME}}/
├── {{TOP_LEVEL_DIR_1}}/
├── {{TOP_LEVEL_DIR_2}}/
├── docs/
│   └── business-knowledge/   ← Layer 2 (project-specific business rules)
└── README.md
```

Reference: [docs/business-knowledge/README.md](../docs/business-knowledge/README.md)

---

## Build / dev / test commands

| Command | Use |
|---|---|
| `{{DEV_CMD}}` | Local dev server |
| `{{TEST_CMD}}` | Unit tests |
| `{{LINT_CMD}}` | Lint |
| `{{BUILD_CMD}}` | Production build |

---

## Project-specific patterns (check before assuming)

### {{PATTERN_NAME_1}}

{{PATTERN_DESCRIPTION_1}}

### Pre-implementation checklist before touching any file

1. **i18n** — does this app use translations? (Y/N → which library)
2. **Env vars** — read the actual `.env.local` / config before assuming
3. **Existing patterns** — read 2-3 nearby files in the same directory before introducing new patterns

---

## Key reference files

| File | Purpose | Read before… |
|---|---|---|
| `{{KEY_FILE_1}}` | {{PURPOSE_1}} | {{TRIGGER_1}} |
| `{{KEY_FILE_2}}` | {{PURPOSE_2}} | {{TRIGGER_2}} |
| `docs/business-knowledge/` | Layer 2 (business rules) | Any non-trivial feature work |
| `~/.claude/skills/engineering-craft/INDEX.md` | Layer 1 (engineering patterns) | Any security/concurrency/config work |

---

## Documentation requirement

**Core principle**: docs stay in sync with code at all times.

Project-specific:
- Every push that includes substantive non-test code changes MUST also touch at least one tracked `*.md` file
- New invariants → write a Layer 2 doc in `docs/business-knowledge/`
- Recurring bug pattern → captured by `~/.claude/hooks/post-codex-fix-extract-lesson.sh` on the next review-fix commit; surfaced to engineering-craft via `/dev-pipeline:consolidate-lessons`

---

## Commit message format

```
feat(scope): short description

- MIU 1: [what] - [why it matters]
- Tests: what was verified

Co-Authored-By: Claude <noreply@anthropic.com>
```

**Scopes**: {{COMMIT_SCOPES}}

---

## What this file deliberately does NOT contain

The following live elsewhere — defer to them, don't duplicate:

- **MIU workflow** → `dev-pipeline:miu-methodology` skill
- **Validation gate** → `/dev-pipeline:validate`
- **Deliver gate** → `/dev-pipeline:deliver`
- **Defensive engineering patterns** (concurrency, enumeration safety, config drift, etc.) → `engineering-craft` skill (auto-loaded based on diff keywords)
- **Workflow disciplines** (branch from main, self-review, push-back) → `engineering-craft/categories/workflow/`
- **Library-choice reflexes** → `engineering-craft/categories/library-choice/`
- **Pre-commit / pre-push hooks** → installed by `~/.claude/setup-git-hooks.sh`

If a flow you need is missing from these layers, raise it as a gap to be added — do NOT inline it here.

---

*Generated from `engineering-craft/bootstrap/templates/project-CLAUDE.md` — replace placeholders, commit to your project root at `.claude/CLAUDE.md`.*
